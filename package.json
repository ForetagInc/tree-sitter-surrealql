{
	"name": "tree-sitter-surreal",
	"version": "0.1.3",
	"module": "index.ts",
	"type": "module",
	"private": true,
	"scripts": {
		"gen": "tree-sitter generate",
		"build": "tree-sitter build",
		"test": "tree-sitter test",
		"parse": "tree-sitter parse",
		"compare": "bun run compare.ts",
		"qa": "biome format --write . && biome check --write ."
	},
	"devDependencies": {
		"@biomejs/biome": "^2.4.12",
		"@types/bun": "latest",
		"tree-sitter-cli": "^0.26.8"
	},
	"peerDependencies": {
		"typescript": "^5.9.3"
	}
}
